# ClaimsCo Document Tools — Python dependencies
# Run ClaimsCo_Tools_Setup.bat to install all of these automatically.

# ── Existing (Claim File Renamer) ─────────────────────────────────────────────
PySide6>=6.6.0
PyMuPDF>=1.24.0
pyinstaller>=6.0.0

# ── Tab 2: Privacy Redaction ──────────────────────────────────────────────────
# PyMuPDF is already listed above — it handles redaction natively.

# ── Tab 3: PDF Extraction ─────────────────────────────────────────────────────
pdfplumber>=0.10.0
pytesseract>=0.3.10
pdf2image>=1.16.3
Pillow>=10.0.0
python-docx>=1.1.0
